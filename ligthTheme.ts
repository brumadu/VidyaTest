import {DefaultTheme} from 'styled-components/native';

const lightTheme: DefaultTheme = {
  colors: {
    background: '#FFFFFF',
    primaryButton: '#006FFD',
    secondaryButton: '#F8F9FE',
    primaryText: '#1F2024',
    secondaryText: '#71727A',
  },
};

export {lightTheme};
